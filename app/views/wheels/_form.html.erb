<%= form_for(@wheel) do |f| %>
  <% if @wheel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wheel.errors.count, "error") %> prohibited this wheel from being saved:</h2>

      <ul>
      <% @wheel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :car_id %><br>
    <%= f.text_field :car_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
